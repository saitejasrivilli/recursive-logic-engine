"""RLVR (Reward and Loss Velocity Regularization) loss functions."""

import torch\nfrom typing import Dict, List, Deque\nfrom collections import deque\nimport logging\n\n\nlogger = logging.getLogger(__name__)\n\n\nclass VelocityTracker:\n    \"\"\"Track velocity of metrics to detect training instability.\"\"\"\n    \n    def __init__(self, window_size: int = 10):\n        \"\"\"\n        Initialize velocity tracker.\n        \n        Args:\n            window_size: Number of steps to track for velocity computation\n        \"\"\"\n        self.window_size = window_size\n        self.loss_history: Deque = deque(maxlen=window_size)\n        self.reward_history: Deque = deque(maxlen=window_size)\n    \n    def update(self, loss: float, reward: float):\n        \"\"\"Update tracker with new metrics.\"\"\"\n        self.loss_history.append(loss)\n        self.reward_history.append(reward)\n    \n    def compute_loss_velocity(self) -> float:\n        \"\"\"\n        Compute velocity (rate of change) of loss.\n        \n        Returns:\n            Loss velocity (higher = more unstable)\n        \"\"\"\n        if len(self.loss_history) < 2:\n            return 0.0\n        \n        history_list = list(self.loss_history)\n        velocities = [abs(history_list[i] - history_list[i-1]) \n                      for i in range(1, len(history_list))]\n        \n        return sum(velocities) / len(velocities)\n    \n    def compute_reward_velocity(self) -> float:\n        \"\"\"\n        Compute velocity (rate of change) of reward.\n        \n        Returns:\n            Reward velocity (higher = more unstable)\n        \"\"\"\n        if len(self.reward_history) < 2:\n            return 0.0\n        \n        history_list = list(self.reward_history)\n        velocities = [abs(history_list[i] - history_list[i-1]) \n                      for i in range(1, len(history_list))]\n        \n        return sum(velocities) / len(velocities)\n    \n    def get_metrics(self) -> Dict[str, float]:\n        \"\"\"Get current velocity metrics.\"\"\"\n        return {\n            \"loss_velocity\": self.compute_loss_velocity(),\n            \"reward_velocity\": self.compute_reward_velocity(),\n        }\n\n\nclass RLVRLoss:\n    \"\"\"Reward and Loss Velocity Regularization.\"\"\"\n    \n    def __init__(\n        self,\n        loss_velocity_coeff: float = 0.01,\n        reward_velocity_coeff: float = 0.01,\n    ):\n        \"\"\"\n        Initialize RLVR loss.\n        \n        Args:\n            loss_velocity_coeff: Coefficient for loss velocity penalty\n            reward_velocity_coeff: Coefficient for reward velocity penalty\n        \"\"\"\n        self.loss_velocity_coeff = loss_velocity_coeff\n        self.reward_velocity_coeff = reward_velocity_coeff\n        self.velocity_tracker = VelocityTracker(window_size=10)\n    \n    def compute_velocity_penalty(self) -> torch.Tensor:\n        \"\"\"\n        Compute RLVR penalty from velocity metrics.\n        \n        Higher velocity = higher penalty, discouraging instability.\n        \n        Returns:\n            Velocity penalty loss\n        \"\"\"\n        loss_vel = self.velocity_tracker.compute_loss_velocity()\n        reward_vel = self.velocity_tracker.compute_reward_velocity()\n        \n        # Clamp to prevent exploding penalties\n        loss_vel = min(loss_vel, 10.0)\n        reward_vel = min(reward_vel, 10.0)\n        \n        penalty = (\n            self.loss_velocity_coeff * loss_vel +\n            self.reward_velocity_coeff * reward_vel\n        )\n        \n        return torch.tensor(penalty, dtype=torch.float32)\n    \n    def step(\n        self,\n        loss: float,\n        reward: float,\n    ) -> Dict[str, float]:\n        \"\"\"\n        Update tracker and compute penalty.\n        \n        Args:\n            loss: Current loss value\n            reward: Current reward value\n            \n        Returns:\n            Dict with velocity metrics\n        \"\"\"\n        self.velocity_tracker.update(loss, reward)\n        penalty = self.compute_velocity_penalty()\n        \n        return {\n            \"velocity_penalty\": penalty.item(),\n            **self.velocity_tracker.get_metrics(),\n        }\n\n\nclass TrainingStabilityMonitor:\n    \"\"\"Monitor and enforce training stability constraints.\"\"\"\n    \n    def __init__(\n        self,\n        reward_spike_threshold: float = 0.5,  # Absolute change threshold\n        loss_spike_threshold: float = 0.5,\n        velocity_threshold: float = 1.0,\n    ):\n        \"\"\"\n        Initialize stability monitor.\n        \n        Args:\n            reward_spike_threshold: Max allowed absolute change in reward\n            loss_spike_threshold: Max allowed absolute change in loss\n            velocity_threshold: Max allowed velocity before warning\n        \"\"\"\n        self.reward_spike_threshold = reward_spike_threshold\n        self.loss_spike_threshold = loss_spike_threshold\n        self.velocity_threshold = velocity_threshold\n        \n        self.last_reward = None\n        self.last_loss = None\n        self.stability_violations = 0\n    \n    def check_stability(\n        self,\n        current_loss: float,\n        current_reward: float,\n        step: int,\n    ) -> Dict[str, any]:\n        \"\"\"\n        Check if training is stable.\n        \n        Args:\n            current_loss: Current loss value\n            current_reward: Current reward value\n            step: Current training step\n            \n        Returns:\n            Dict with stability checks\n        \"\"\"\n        result = {\n            \"is_stable\": True,\n            \"warnings\": [],\n            \"step\": step,\n        }\n        \n        if self.last_loss is not None:\n            # Check loss spike\n            loss_delta = abs(current_loss - self.last_loss)\n            if loss_delta > self.loss_spike_threshold:\n                result[\"is_stable\"] = False\n                result[\"warnings\"].append(\n                    f\"Loss spike detected: {loss_delta:.4f} (threshold: {self.loss_spike_threshold})\"\n                )\n                self.stability_violations += 1\n        \n        if self.last_reward is not None:\n            # Check reward spike\n            reward_delta = abs(current_reward - self.last_reward)\n            if reward_delta > self.reward_spike_threshold:\n                result[\"is_stable\"] = False\n                result[\"warnings\"].append(\n                    f\"Reward spike detected: {reward_delta:.4f} (threshold: {self.reward_spike_threshold})\"\n                )\n                self.stability_violations += 1\n        \n        # Update history\n        self.last_loss = current_loss\n        self.last_reward = current_reward\n        \n        return result\n\n\ndef combined_rlvr_loss(\n    base_loss: torch.Tensor,\n    velocity_penalty: torch.Tensor,\n    coeff: float = 1.0,\n) -> torch.Tensor:\n    \"\"\"\n    Combine base loss with RLVR penalty.\n    \n    Total Loss = base_loss + coeff * velocity_penalty\n    \n    Args:\n        base_loss: Base training loss\n        velocity_penalty: RLVR velocity penalty\n        coeff: Weight for velocity penalty\n        \n    Returns:\n        Combined loss\n    \"\"\"\n    return base_loss + coeff * velocity_penalty\n