"""Evaluation module with self-correction analysis."""

import torch\nimport logging\nfrom typing import List, Dict, Optional, Tuple\nfrom dataclasses import dataclass\n\n\nlogger = logging.getLogger(__name__)\n\n\n@dataclass\nclass SelfCorrectionTrace:\n    \"\"\"Trace of a single self-correction instance.\"\"\"\n    input: str\n    initial_think: str\n    correction: Optional[str] = None\n    final_think: Optional[str] = None\n    output: str = \"\"\n    ground_truth: str = \"\"\n    is_correct: bool = False\n    \n    def to_dict(self) -> Dict:\n        return {\n            \"input\": self.input,\n            \"initial_think\": self.initial_think,\n            \"correction\": self.correction,\n            \"final_think\": self.final_think,\n            \"output\": self.output,\n            \"ground_truth\": self.ground_truth,\n            \"is_correct\": self.is_correct,\n        }\n\n\nclass SelfCorrectionAnalyzer:\n    \"\"\"Analyze and extract self-correction traces.\"\"\"\n    \n    @staticmethod\n    def extract_thinking_blocks(text: str) -> Tuple[str, str]:\n        \"\"\"\n        Extract thinking block and final answer from text.\n        \n        Args:\n            text: Full model output\n            \n        Returns:\n            Tuple of (thinking_block, final_answer)\n        \"\"\"\n        if \"<think>\" in text and \"</think>\" in text:\n            start = text.find(\"<think>\") + len(\"<think>\")\n            end = text.find(\"</think>\")\n            thinking = text[start:end].strip()\n            final = text[end+len(\"</think>\"):].strip()\n            return thinking, final\n        else:\n            return \"\", text.strip()\n    \n    @staticmethod\n    def detect_correction(thinking_block: str) -> Optional[str]:\n        \"\"\"\n        Detect if a correction was made in the thinking block.\n        \n        Looks for correction indicators like \"Wait...\", \"Actually...\", \"Hmm...\", etc.\n        \n        Args:\n            thinking_block: Content of thinking block\n            \n        Returns:\n            Correction text if detected, None otherwise\n        \"\"\"\n        correction_phrases = [\n            \"wait\",\n            \"actually\",\n            \"hmm\",\n            \"let me reconsider\",\n            \"i was wrong\",\n            \"i made a mistake\",\n            \"correction\",\n            \"scratch that\",\n        ]\n        \n        thinking_lower = thinking_block.lower()\n        \n        for phrase in correction_phrases:\n            if phrase in thinking_lower:\n                # Find the position of the correction phrase\n                idx = thinking_lower.find(phrase)\n                # Return text from correction point onward\n                return thinking_block[idx:].strip()\n        \n        return None\n    \n    @staticmethod\n    def create_trace(\n        input_text: str,\n        full_output: str,\n        ground_truth: str,\n        is_correct: bool,\n    ) -> SelfCorrectionTrace:\n        \"\"\"\n        Create a self-correction trace from model output.\n        \n        Args:\n            input_text: Input prompt\n            full_output: Full model output (with thinking blocks)\n            ground_truth: Ground truth answer\n            is_correct: Whether the final answer is correct\n            \n        Returns:\n            SelfCorrectionTrace instance\n        \"\"\"\n        thinking, final = SelfCorrectionAnalyzer.extract_thinking_blocks(full_output)\n        correction = SelfCorrectionAnalyzer.detect_correction(thinking)\n        \n        # Split thinking into initial and final if correction was made\n        initial_think = thinking\n        final_think = thinking\n        \n        if correction:\n            initial_think = thinking[:thinking.find(correction)].strip()\n            final_think = correction\n        \n        trace = SelfCorrectionTrace(\n            input=input_text,\n            initial_think=initial_think,\n            correction=correction,\n            final_think=final_think,\n            output=final,\n            ground_truth=ground_truth,\n            is_correct=is_correct,\n        )\n        \n        return trace\n\n\nclass CatastrophicForgettingEvaluator:\n    \"\"\"Evaluate catastrophic forgetting on general knowledge tasks.\"\"\"\n    \n    @staticmethod\n    def evaluate(\n        model,\n        general_knowledge_loader,\n        device: str = \"cuda\" if torch.cuda.is_available() else \"cpu\",\n    ) -> Dict[str, float]:\n        \"\"\"\n        Evaluate model on general knowledge (MMLU-style tasks).\n        \n        Args:\n            model: Model to evaluate\n            general_knowledge_loader: DataLoader for general knowledge tasks\n            device: Device for evaluation\n            \n        Returns:\n            Dict with metrics\n        \"\"\"\n        model.eval()\n        correct = 0\n        total = 0\n        \n        with torch.no_grad():\n            for batch in general_knowledge_loader:\n                input_ids = batch[\"input_ids\"].to(device)\n                attention_mask = batch[\"attention_mask\"].to(device)\n                ground_truth = batch[\"ground_truth\"]\n                \n                outputs = model(\n                    input_ids=input_ids,\n                    attention_mask=attention_mask,\n                )\n                logits = outputs.logits\n                \n                # Simplified: check if top token matches first token of answer\n                predictions = torch.argmax(logits[:, -1, :], dim=-1)\n                \n                # For proper evaluation, need tokenized ground truth\n                # This is simplified version\n                correct += (predictions == 1).sum().item()  # Dummy logic\n                total += len(ground_truth)\n        \n        accuracy = correct / total if total > 0 else 0.0\n        \n        logger.info(f\"General Knowledge Accuracy: {accuracy:.4f}\")\n        \n        return {\"general_knowledge_accuracy\": accuracy}\n\n\nclass LogicPuzzleEvaluator:\n    \"\"\"Evaluate model on logic puzzle benchmark.\"\"\"\n    \n    @staticmethod\n    def evaluate(\n        model,\n        logic_puzzle_loader,\n        device: str = \"cuda\" if torch.cuda.is_available() else \"cpu\",\n    ) -> Dict[str, float]:\n        \"\"\"\n        Evaluate model on logic puzzles.\n        \n        Args:\n            model: Model to evaluate\n            logic_puzzle_loader: DataLoader for logic puzzles\n            device: Device for evaluation\n            \n        Returns:\n            Dict with metrics\n        \"\"\"\n        model.eval()\n        correct = 0\n        total = 0\n        self_corrections = 0\n        traces = []\n        \n        with torch.no_grad():\n            for batch in logic_puzzle_loader:\n                input_ids = batch[\"input_ids\"].to(device)\n                attention_mask = batch[\"attention_mask\"].to(device)\n                ground_truth = batch[\"ground_truth\"]\n                example_ids = batch.get(\"example_id\", [])\n                \n                outputs = model(\n                    input_ids=input_ids,\n                    attention_mask=attention_mask,\n                )\n                logits = outputs.logits\n                \n                # Decode outputs\n                predictions = torch.argmax(logits[:, -1, :], dim=-1)\n                \n                for pred, target in zip(predictions, ground_truth):\n                    is_correct = pred == 1  # Dummy check\n                    correct += int(is_correct)\n                    total += 1\n                    \n                    # Create trace\n                    trace = SelfCorrectionTrace(\n                        input=\"logic_puzzle\",\n                        initial_think=\"Analyzing...\",\n                        output=str(pred),\n                        ground_truth=str(target),\n                        is_correct=is_correct,\n                    )\n                    traces.append(trace)\n        \n        accuracy = correct / total if total > 0 else 0.0\n        self_correction_rate = self_corrections / total if total > 0 else 0.0\n        \n        logger.info(f\"Logic Puzzle Accuracy: {accuracy:.4f}\")\n        logger.info(f\"Self-Correction Rate: {self_correction_rate:.4f}\")\n        \n        return {\n            \"logic_puzzle_accuracy\": accuracy,\n            \"self_correction_rate\": self_correction_rate,\n            \"traces\": traces,\n        }\n\n\ndef compare_models(\n    base_model,\n    aligned_model,\n    test_loader,\n    device: str = \"cuda\" if torch.cuda.is_available() else \"cpu\",\n) -> Dict[str, any]:\n    \"\"\"\n    Compare base model vs aligned model.\n    \n    Args:\n        base_model: Base (untrained) model\n        aligned_model: Aligned (trained) model\n        test_loader: Test dataloader\n        device: Device\n        \n    Returns:\n        Comparison dict\n    \"\"\"\n    def evaluate_model(model, loader):\n        model.eval()\n        correct = 0\n        total = 0\n        \n        with torch.no_grad():\n            for batch in loader:\n                input_ids = batch[\"input_ids\"].to(device)\n                outputs = model(input_ids=input_ids)\n                logits = outputs.logits\n                \n                predictions = torch.argmax(logits[:, -1, :], dim=-1)\n                ground_truth = batch[\"ground_truth\"]\n                \n                correct += (predictions == 1).sum().item()  # Dummy\n                total += len(ground_truth)\n        \n        return correct / total if total > 0 else 0.0\n    \n    base_acc = evaluate_model(base_model, test_loader)\n    aligned_acc = evaluate_model(aligned_model, test_loader)\n    \n    return {\n        \"base_model_accuracy\": base_acc,\n        \"aligned_model_accuracy\": aligned_acc,\n        \"improvement\": aligned_acc - base_acc,\n        \"improvement_percent\": (aligned_acc - base_acc) / base_acc * 100 if base_acc > 0 else 0,\n    }\n